create table users(
    id integer primary key ,
    name text,
    surname text,
    birthday date
);

create table cars(
                      id integer primary key ,
                      brand text,
                      serialNumber text
);